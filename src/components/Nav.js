import React from 'react';
import { Link } from 'react-router-dom';

const Nav = () => {
    return (
        <div>
          <Link to="/" className="links">Home </Link>
        </div>
    );
  }
  
  export default Nav;